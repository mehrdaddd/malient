import { SimpleHasher } from './simpleHasher';
import { WorkerHasher } from './workerHasher';
var hasher = null;
export var destroyHasher = function () { return (hasher = null); };
export var createHasher = function () {
    var numWorkers = 3;
    if (!hasher) {
        try {
            hasher = new WorkerHasher(numWorkers);
        }
        catch (error) {
            hasher = new SimpleHasher();
        }
    }
    return hasher;
};
//# sourceMappingURL=hasherCreator.js.map