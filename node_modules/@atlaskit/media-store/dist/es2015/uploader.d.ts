import { ChunkinatorFile } from 'chunkinator';
import { MediaApiConfig } from './models/auth';
export declare type UploadableFile = {
    content: ChunkinatorFile;
    name?: string;
    mimeType?: string;
    collection?: string;
};
export declare type UploadFileCallbacks = {
    onProgress: (progress: number) => void;
};
export interface UploadFileResult {
    deferredFileId: Promise<string>;
    cancel: () => void;
}
export declare const uploadFile: (file: UploadableFile, config: MediaApiConfig, callbacks?: UploadFileCallbacks | undefined) => UploadFileResult;
