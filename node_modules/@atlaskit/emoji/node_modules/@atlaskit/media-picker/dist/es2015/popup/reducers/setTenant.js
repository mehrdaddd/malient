import * as tslib_1 from "tslib";
import { SET_TENANT } from '../actions/setTenant';
export default function setTenant(state, action) {
    if (action.type === SET_TENANT) {
        return tslib_1.__assign({}, state, { tenant: action.tenant });
    }
    else {
        return state;
    }
}
//# sourceMappingURL=setTenant.js.map