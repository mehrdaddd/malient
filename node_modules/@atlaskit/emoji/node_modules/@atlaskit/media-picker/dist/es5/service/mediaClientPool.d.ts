import { MediaClient } from './mediaClient';
import { AuthProvider } from '@atlaskit/media-core';
export declare class MediaClientPool {
    private readonly apiUrl;
    private readonly authProvider;
    private clients;
    private clientForAbsentCollection?;
    constructor(apiUrl: string, authProvider: AuthProvider);
    getMediaClient: (collection?: string | undefined) => MediaClient;
    private createNewClient;
    private getStoredClient;
    private setClient;
}
