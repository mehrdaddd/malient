import { MediaApiConfig } from '@atlaskit/media-store';
import { RemoteMediaCollectionProvider } from './remoteMediaCollectionProvider';
import { CollectionService, SortDirection } from '../services/collectionService';
import { Pool } from './util/pool';
export declare class RemoteMediaCollectionProviderFactory {
    static fromCollectionService(collectionService: CollectionService, collectionName: string, pageSize: number, sortDirection?: SortDirection): RemoteMediaCollectionProvider;
    static fromMediaAPI(config: MediaApiConfig, collectionName: string, pageSize: number, sortDirection?: SortDirection): RemoteMediaCollectionProvider;
    static fromPool(pool: Pool<RemoteMediaCollectionProvider>, config: MediaApiConfig, collectionName: string, pageSize: number, sortDirection?: SortDirection): RemoteMediaCollectionProvider;
    static createPool(): Pool<RemoteMediaCollectionProvider>;
}
