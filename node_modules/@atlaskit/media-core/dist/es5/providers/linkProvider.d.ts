import { LinkItem, MediaApiConfig } from '../';
import { LinkService } from '../services/linkService';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/publishReplay';
export interface LinkProvider {
    observable(): Observable<LinkItem>;
}
export declare class LinkProvider {
    static fromMediaApi(config: MediaApiConfig, linkId: string, collectionName?: string): LinkProvider;
    static fromLinkService(linkService: LinkService, linkId: string, collectionName?: string): LinkProvider;
}
