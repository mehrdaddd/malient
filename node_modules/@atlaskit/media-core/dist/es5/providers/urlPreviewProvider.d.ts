import { UrlPreview, MediaApiConfig } from '../';
import { UrlPreviewService } from '../services/urlPreviewService';
import { Observable } from 'rxjs/Observable';
export interface UrlPreviewProvider {
    observable(): Observable<UrlPreview>;
}
export declare class UrlPreviewProvider {
    static fromMediaApi(config: MediaApiConfig, url: string): UrlPreviewProvider;
    static fromUrlPreviewService(urlPreviewService: UrlPreviewService, url: string): UrlPreviewProvider;
}
