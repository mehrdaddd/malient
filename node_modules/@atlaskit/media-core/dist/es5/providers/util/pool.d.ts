export declare class Pool<Item> {
    private readonly referenceCountById;
    private readonly referencesById;
    acquire(poolId: string, createItem: () => Item): Item;
    release(poolId: string): void;
}
