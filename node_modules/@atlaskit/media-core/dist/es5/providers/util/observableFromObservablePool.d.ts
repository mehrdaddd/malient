import { Observable } from 'rxjs/Observable';
import { Pool } from './pool';
export declare function observableFromObservablePool<Result>(pool: Pool<Observable<Result>>, poolId: string, createObservable: () => Observable<Result>): Observable<Result>;
