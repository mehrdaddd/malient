import { FileItem, MediaApiConfig } from '../';
import { FileService } from '../services/fileService';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/publishReplay';
import { LRUCache } from 'lru-fast';
export declare const FILE_PROVIDER_RETRY_INTERVAL = 2000;
export interface FileProvider {
    observable(): Observable<FileItem>;
}
export declare class FileProvider {
    static fromMediaApi(config: MediaApiConfig, fileItemCache: LRUCache<string, FileItem>, fileId: string, collection?: string, pollInterval?: number): FileProvider;
    static fromFileService(fileService: FileService, fileId: string, collectionName?: string, pollInterval?: number): FileProvider;
}
