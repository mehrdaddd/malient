import { AxiosError } from 'axios';
import { MediaCollection } from './collection';
export interface FetchingCollectionSucceeded {
    type: 'FETCHING_COLLECTION_SUCCEEDED';
    collection: MediaCollection;
    hasNextPage: boolean;
}
export interface FetchingCollectionFailed {
    type: 'FETCHING_COLLECTION_FAILED';
    error: AxiosError;
}
export declare function fetchingCollectionSucceeded(collection: MediaCollection, hasNextPage: boolean): FetchingCollectionSucceeded;
export declare function fetchingCollectionFailed(error: AxiosError): FetchingCollectionFailed;
export declare type Action = FetchingCollectionSucceeded | FetchingCollectionFailed;
