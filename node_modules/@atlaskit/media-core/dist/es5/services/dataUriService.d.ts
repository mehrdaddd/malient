import { AuthProvider } from '@atlaskit/media-store';
import { MediaItem } from '../';
import { FetchImageOptions } from './blobService';
export declare type DataUri = string;
export interface DataUriService {
    fetchOriginalDataUri(mediaItem: MediaItem): Promise<DataUri>;
    fetchImageDataUri(mediaItem: MediaItem, options: FetchImageOptions): Promise<DataUri>;
}
export declare class MediaDataUriService implements DataUriService {
    readonly authProvider: AuthProvider;
    readonly serviceHost: string;
    readonly collectionName: string | undefined;
    private blobService;
    constructor(authProvider: AuthProvider, serviceHost: string, collectionName?: string | undefined);
    fetchOriginalDataUri(mediaItem: MediaItem): Promise<DataUri>;
    fetchImageDataUri(mediaItem: MediaItem, options: FetchImageOptions): Promise<DataUri>;
    private readBlob(blob);
}
