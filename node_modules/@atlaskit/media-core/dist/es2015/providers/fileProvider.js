import { MediaFileService } from '../services/fileService';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/publishReplay';
export var FILE_PROVIDER_RETRY_INTERVAL = 2000;
var FileProvider = /** @class */ (function () {
    function FileProvider() {
    }
    FileProvider.fromMediaApi = function (config, fileItemCache, fileId, collection, pollInterval) {
        return FileProvider.fromFileService(new MediaFileService(config, fileItemCache), fileId, collection, pollInterval);
    };
    FileProvider.fromFileService = function (fileService, fileId, collectionName, pollInterval) {
        return {
            observable: function () {
                var observable = new Observable(function (subscriber) {
                    var handle;
                    var timeout = pollInterval || 1000;
                    var fetch = function () {
                        fileService.getFileItem(fileId, collectionName).then(function (fileItem) {
                            if (fileItem.details.processingStatus !== 'pending') {
                                subscriber.next(fileItem);
                                subscriber.complete();
                            }
                            else {
                                subscriber.next(fileItem);
                                handle = window.setTimeout(function () { return fetch(); }, timeout);
                            }
                        }, function (error) {
                            subscriber.error(error);
                        });
                    };
                    fetch();
                    return function () {
                        if (handle !== null) {
                            clearTimeout(handle);
                        }
                    };
                }).publishReplay(1);
                observable.connect();
                return observable;
            },
        };
    };
    return FileProvider;
}());
export { FileProvider };
//# sourceMappingURL=fileProvider.js.map