import { MediaCollectionProvider } from '../mediaCollectionProvider';
import { Pool } from './pool';
export declare function mediaCollectionProviderFromPool<T extends MediaCollectionProvider>(pool: Pool<T>, poolId: string, createProvider: () => T): T;
