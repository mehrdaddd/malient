import { MediaApiConfig, UrlPreview } from '../';
import { Observable } from 'rxjs/Observable';
import { UrlPreviewProvider } from './urlPreviewProvider';
import { Pool } from './util/pool';
export declare type MediaUrlPreviewObservablePool = Pool<Observable<UrlPreview>>;
export declare class MediaUrlPreviewProvider extends UrlPreviewProvider {
    static fromMediaApi(config: MediaApiConfig, url: string): UrlPreviewProvider;
    static fromPool(pool: MediaUrlPreviewObservablePool, config: MediaApiConfig, url: string): {
        observable(): Observable<UrlPreview>;
    };
    static createPool(): MediaUrlPreviewObservablePool;
}
