import { FileItem, MediaApiConfig } from '../';
import { LRUCache } from 'lru-fast';
export interface FileService {
    getFileItem(fileId: string, collection?: string): Promise<FileItem>;
}
export declare class MediaFileService implements FileService {
    private config;
    private fileItemCache;
    constructor(config: MediaApiConfig, fileItemCache: LRUCache<string, FileItem>);
    getFileItem(fileId: string, collectionName?: string): Promise<FileItem>;
}
