var ContentNodeView = /** @class */ (function () {
    function ContentNodeView(node, view, elementType) {
        if (elementType === void 0) { elementType = 'div'; }
        var _this = this;
        this.handleRef = function (node) { return _this._handleRef(node); };
        if (view.dom.parentNode) {
            this.contentDOM = view.dom.parentNode.appendChild(document.createElement(elementType));
            // @see ED-3790
            // something gets messed up during mutation processing inside of a nodeView if DOM structure has nested plain "div"s,
            // it doesn't see the difference between them and it kills the nodeView
            this.contentDOM.className = node.type.name + "View-content-wrap";
        }
    }
    ContentNodeView.prototype._handleRef = function (node) {
        var contentDOM = this.contentDOM;
        if (node && contentDOM && !node.contains(contentDOM)) {
            node.appendChild(contentDOM);
        }
    };
    ContentNodeView.prototype.destroy = function () {
        this.contentDOM = undefined;
    };
    return ContentNodeView;
}());
export default ContentNodeView;
//# sourceMappingURL=contentNodeView.js.map