/// <reference types="react" />
import { PureComponent } from 'react';
import { MediaType, CardEventClickHandler, ProviderFactory } from '@atlaskit/editor-common';
import { CardDimensions, CardEventHandler } from '@atlaskit/media-card';
import { ImageResizeMode } from '@atlaskit/media-core';
import { MediaStateManager } from '../../pm-plugins/main';
import { Appearance } from './MediaComponent';
export interface Props {
    id: string;
    tempId: string;
    providers?: ProviderFactory;
    type: MediaType;
    collection: string;
    cardDimensions?: CardDimensions;
    resizeMode?: ImageResizeMode;
    onClick?: CardEventClickHandler;
    onDelete?: CardEventHandler;
    appearance?: Appearance;
    stateManagerFallback?: MediaStateManager;
    selected: boolean;
    url?: string;
    onExternalImageLoaded?: (dimensions: {
        width: number;
        height: number;
    }) => void;
    disableOverlay?: boolean;
}
export default class MediaItem extends PureComponent<Props, {}> {
    private providerFactory;
    constructor(props: any);
    componentWillUnmount(): void;
    private renderWithProvider;
    render(): JSX.Element;
}
