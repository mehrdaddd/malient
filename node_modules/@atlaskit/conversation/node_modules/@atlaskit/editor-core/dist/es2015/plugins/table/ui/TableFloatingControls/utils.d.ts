import { EditorState } from 'prosemirror-state';
export declare const getLineMarkerWidth: (tableElement: HTMLElement, scroll: number) => number;
export declare type TableSelection = {
    startIdx: number | null;
    endIdx: number | null;
    count: number;
    hasMultipleSelection: boolean;
    inSelection: (idx: number) => boolean;
    frontOfSelection: (idx: number) => boolean;
};
export declare const findRowSelection: (state: EditorState, elems: NodeListOf<HTMLTableRowElement>) => TableSelection;
export declare const findColumnSelection: (state: EditorState, elems: HTMLCollection) => TableSelection;
