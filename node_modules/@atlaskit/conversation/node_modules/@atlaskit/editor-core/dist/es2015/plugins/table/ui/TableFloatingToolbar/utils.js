import { browser } from '@atlaskit/editor-common';
export function getShortcut(keymap) {
    if (keymap) {
        var shortcutMap = { Cmd: '⌘', Shift: '⇧', Ctrl: '^', Alt: '⌥' };
        var shortcut = browser.mac ? keymap.mac : keymap.windows;
        var arr = shortcut.split('-');
        return shortcutMap[arr[0]] + " + " + arr[1].toUpperCase();
    }
}
//# sourceMappingURL=utils.js.map