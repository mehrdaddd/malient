/// <reference types="react" />
import * as React from 'react';
import { Schema } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
export interface Format {
    name: string;
    type: string;
    keymap?: Function;
    autoFormatting?: Function;
}
export declare const formatting: Format[];
export declare const getSupportedFormatting: (schema: Schema) => Format[];
export declare const getComponentFromKeymap: (keymap: any) => any;
export interface Props {
    editorView: EditorView;
    isVisible: boolean;
    appearance?: string;
}
export default class HelpDialog extends React.Component<Props, any> {
    private formatting;
    constructor(props: any);
    closeDialog: () => void;
    handleEsc: (e: any) => void;
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element | null;
}
