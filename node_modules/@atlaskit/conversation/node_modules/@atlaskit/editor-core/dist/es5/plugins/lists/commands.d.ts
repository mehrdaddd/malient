import { EditorState, Transaction } from 'prosemirror-state';
import { EditorView } from 'prosemirror-view';
/**
 * Function will lift list item following selection to level-1.
 */
export declare function liftFollowingList(state: EditorState, from: number, to: number, rootListDepth: number, tr: Transaction): Transaction;
export declare const enterKeyCommand: (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
export declare const backspaceKeyCommand: (p1: EditorState, p2?: ((tr: Transaction) => void) | undefined, p3?: EditorView | undefined) => boolean;
export declare const toggleList: (state: EditorState, dispatch: (tr: Transaction) => void, view: EditorView, listType: "bulletList" | "orderedList") => boolean;
