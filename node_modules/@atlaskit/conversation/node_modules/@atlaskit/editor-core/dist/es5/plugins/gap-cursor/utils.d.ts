import { Node as PMNode } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
export declare const isIgnored: (node?: PMNode | null | undefined) => boolean;
export declare const fixCursorAlignment: (view: EditorView) => void;
