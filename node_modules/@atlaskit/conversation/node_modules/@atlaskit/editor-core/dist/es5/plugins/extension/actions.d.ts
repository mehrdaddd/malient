import { EditorState, Transaction } from 'prosemirror-state';
import { EditorView } from 'prosemirror-view';
import { Slice } from 'prosemirror-model';
import { MacroProvider } from '../macro';
export declare const setExtensionElement: (element: HTMLElement | null) => (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
export declare const editExtension: (macroProvider: MacroProvider | null) => (view: EditorView) => boolean;
export declare const removeExtension: (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
export declare const removeBodiedExtensionWrapper: (state: EditorState, slice: Slice) => Slice;
export declare const removeBodiedExtensionsIfSelectionIsInBodiedExtension: (slice: Slice, state: EditorState) => Slice;
