import { Context } from '@atlaskit/media-core';
import { UploadService } from '../service/uploadServiceFactory';
import { UploadEventPayloadMap } from '../domain/uploadEvent';
import { UploadComponent } from './component';
import { MediaPickerContext } from '../domain/context';
import { UploadParams } from '../domain/config';
export interface LocalUploadConfig {
    uploadParams: UploadParams;
    useNewUploadService?: boolean;
}
export declare class LocalUploadComponent<M extends UploadEventPayloadMap = UploadEventPayloadMap> extends UploadComponent<M> {
    protected readonly uploadService: UploadService;
    readonly context: Context;
    config: LocalUploadConfig;
    constructor(analyticsContext: MediaPickerContext, context: Context, config: LocalUploadConfig);
    cancel(uniqueIdentifier?: string): void;
    setUploadParams(uploadParams: UploadParams): void;
    private onFilesAdded;
    private onFilePreviewUpdate;
    private onFileUploading;
    private onFileConverting;
    private onFileConverted;
    private onUploadError;
}
