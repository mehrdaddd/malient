import { Action, Store, Dispatch } from 'redux';
import { State } from '../domain';
import { PopupUploadEventEmitter } from '../../components/popup';
declare const _default: (eventEmitter: PopupUploadEventEmitter) => (store: Store<State>) => (next: Dispatch<State>) => (action: Action) => Action;
export default _default;
