import { Store, Middleware } from 'redux';
import { Fetcher } from '../tools/fetcher/fetcher';
import { GetPreviewAction } from '../actions/getPreview';
import { State } from '../domain';
import { SendUploadEventAction } from '../actions/sendUploadEvent';
export default function (fetcher: Fetcher): Middleware;
export declare function getPreview(fetcher: Fetcher, store: Store<State>, {uploadId, file, collection}: GetPreviewAction): Promise<SendUploadEventAction>;
