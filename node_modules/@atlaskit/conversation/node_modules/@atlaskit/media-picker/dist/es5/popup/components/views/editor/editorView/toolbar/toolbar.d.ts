/// <reference types="react" />
import { Component } from 'react';
import { Tool, Color } from '@atlaskit/media-editor';
export declare type PopupState = 'none' | 'color' | 'lineWidth';
export declare const tools: Tool[];
export interface ToolbarProps {
    readonly color: Color;
    readonly tool: Tool;
    readonly lineWidth: number;
    readonly onSave: () => void;
    readonly onCancel: () => void;
    readonly onToolChanged: (tool: Tool) => void;
    readonly onColorChanged: (color: Color) => void;
    readonly onLineWidthChanged: (lineWidth: number) => void;
}
export interface ToolbarState {
    readonly popup: PopupState;
}
export declare class Toolbar extends Component<ToolbarProps, ToolbarState> {
    constructor(props: ToolbarProps);
    render(): JSX.Element;
    private renderToolButtons();
    private showOrHidePopup(target);
    private renderPopup();
}
