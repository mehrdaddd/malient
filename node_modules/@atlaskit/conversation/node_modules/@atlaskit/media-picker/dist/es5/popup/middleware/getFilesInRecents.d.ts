import { AuthProvider } from '@atlaskit/media-core';
import { Action, Dispatch, Store } from 'redux';
import { Fetcher } from '../tools/fetcher/fetcher';
import { State } from '../domain';
export declare const getFilesInRecents: (fetcher: Fetcher) => (store: Store<State>) => (next: Dispatch<Action>) => (action: Action) => Action;
export declare const requestRecentFiles: (fetcher: Fetcher, userAuthProvider: AuthProvider, store: Store<State>) => Promise<void>;
