/// <reference types="react" />
import * as React from 'react';
import { Conversation as ConversationType } from '../model';
import { SharedProps } from './Comment';
export interface Props extends SharedProps {
    id?: string;
    localId?: string;
    conversation?: ConversationType;
    containerId: string;
    onCreateConversation?: (localId: string, containerId: string, value: any, meta: any) => void;
    isExpanded?: boolean;
    meta?: {
        [key: string]: any;
    };
}
export default class Conversation extends React.PureComponent<Props> {
    static defaultProps: {
        placeholder: string;
    };
    private renderComments();
    private renderEditor();
    private onRetry;
    private onSave;
    render(): JSX.Element;
}
