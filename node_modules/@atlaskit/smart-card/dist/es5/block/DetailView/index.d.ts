/// <reference types="react" />
import * as React from 'react';
import { TextWithTooltip, IconWithTooltip, UserViewModel, DetailViewModel } from '../CardView/ViewModel';
export interface DetailViewProps {
    title?: TextWithTooltip;
    description?: TextWithTooltip;
    icon?: IconWithTooltip;
    user?: UserViewModel;
    users?: UserViewModel[];
    thumbnail?: string;
    details?: DetailViewModel[];
    actions?: React.ReactNode;
}
export interface DetailViewState {
}
export declare class DetailView extends React.Component<DetailViewProps, DetailViewState> {
    el?: HTMLElement;
    renderIcon(): JSX.Element | null;
    renderThumbnail(): JSX.Element | null;
    renderUser(): JSX.Element | null;
    render(): JSX.Element;
}
