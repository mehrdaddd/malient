import * as React from 'react';
import Lozenge from '@atlaskit/lozenge';
import { A, Img, Text, LozengeWrapper } from './styled';
var renderIcon = function (icon) {
    if (icon === undefined) {
        return;
    }
    return React.createElement(Img, { src: icon.url, alt: icon.tooltip });
};
var renderLozenge = function (lozenge) {
    if (lozenge === undefined) {
        return;
    }
    return (React.createElement(LozengeWrapper, null,
        React.createElement(Lozenge, { appearance: lozenge.appearance || 'default', isBold: lozenge.isBold }, lozenge.text)));
};
export function CardView(props) {
    var link = props.link, icon = props.icon, text = props.text, lozenge = props.lozenge;
    return (React.createElement(A, { href: link.href, title: link.title },
        renderIcon(icon),
        React.createElement(Text, null, text),
        renderLozenge(lozenge)));
}
//# sourceMappingURL=index.js.map