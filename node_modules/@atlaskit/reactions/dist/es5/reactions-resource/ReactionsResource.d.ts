import { ReactionsProvider, ReactionSummary, ObjectReactionKey, Reactions, ReactionsState } from './types';
import AbstractReactionsResource from './AbstractReactionsResource';
export interface ReactionsProviderConfig {
    sessionToken?: string;
    baseUrl: string;
    autoPoll?: number;
}
export default class ReactionsResource extends AbstractReactionsResource implements ReactionsProvider {
    private config;
    private inFlightDetailsRequests;
    constructor(config: ReactionsProviderConfig);
    private getHeaders();
    getDetailedReaction(reaction: ReactionSummary): Promise<ReactionSummary>;
    fetchReactionDetails(reaction: ReactionSummary): Promise<ReactionSummary>;
    getReactions(keys: ObjectReactionKey[]): Promise<Reactions>;
    addReaction(containerAri: string, ari: string, emojiId: string): Promise<ReactionsState>;
    deleteReaction(containerAri: string, ari: string, emojiId: string): Promise<ReactionsState>;
}
