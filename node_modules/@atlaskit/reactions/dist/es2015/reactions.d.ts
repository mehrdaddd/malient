/// <reference types="react" />
import { Component } from 'react';
import { EmojiProvider } from '@atlaskit/emoji';
import { ReactionsProvider, ReactionSummary } from './reactions-resource';
export interface OnEmoji {
    (emojiId: string): any;
}
export interface Props {
    ari: string;
    containerAri: string;
    reactionsProvider: ReactionsProvider;
    emojiProvider: Promise<EmojiProvider>;
    onReactionClick: OnEmoji;
    onReactionHover?: Function;
    boundariesElement?: string;
    allowAllEmojis?: boolean;
}
export interface State {
    reactions: ReactionSummary[];
    loading: boolean;
    error: boolean;
}
export default class Reactions extends Component<Props, State> {
    private timeouts;
    private reactionRefs;
    private flashOnMount;
    constructor(props: any);
    private onEmojiClick;
    private onReactionHover;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private flash;
    private getReactionsSortFunction;
    private updateState;
    private hasAlreadyReacted(emojiId);
    private handleReactionPickerSelection;
    private handleReactionRef;
    private getTooltip;
    private renderPicker();
    private renderReaction;
    private renderReactions;
    render(): JSX.Element;
}
