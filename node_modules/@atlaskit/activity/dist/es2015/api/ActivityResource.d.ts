import { ActivityItem, ActivityProvider } from '../types';
export default class ActivityResource implements ActivityProvider {
    private frequentPromise?;
    private recentPromise?;
    private serviceConfig;
    private cloudId;
    constructor(baseUrl: string, cloudId: string);
    getFrequentItems(): Promise<ActivityItem[]>;
    getRecentItems(): Promise<ActivityItem[]>;
    searchRecent(search: string): Promise<ActivityItem[]>;
    private filterItems(items, searchTerm);
}
