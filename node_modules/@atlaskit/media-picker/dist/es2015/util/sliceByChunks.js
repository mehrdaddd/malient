export function sliceByChunks(array, maxSize) {
    var chunks = [];
    for (var i = 0; i < array.length; i += maxSize) {
        chunks.push(array.slice(i, i + maxSize));
    }
    return chunks;
}
//# sourceMappingURL=sliceByChunks.js.map