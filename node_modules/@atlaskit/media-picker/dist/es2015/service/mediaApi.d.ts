import { FileDetails } from '@atlaskit/media-core';
import { MediaClient } from './mediaClient';
import { SourceFile } from '../popup/domain';
export interface MediaArtifact {
    processingStatus: string;
    url?: string;
}
export interface MediaFileData {
    id: string;
    processingStatus?: string;
    artifacts?: {
        [name: string]: MediaArtifact;
    };
}
export declare class MediaApi {
    createUpload(mediaClient: MediaClient): Promise<string>;
    appendChunksToUpload(mediaClient: MediaClient, uploadId: string, chunkIds: Array<string>, offset: number): Promise<void>;
    createFileFromUpload(mediaClient: MediaClient, fileName: string, mimeType: string, uploadId: string, collection?: string): Promise<string>;
    pollForFileMetadata(mediaClient: MediaClient, fileId: string, collection?: string): Promise<FileDetails>;
    copyFileToCollection(mediaClient: MediaClient, sourceFile: SourceFile, collection: string): Promise<void>;
}
