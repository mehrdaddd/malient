import { Dispatch, MiddlewareAPI, Action } from 'redux';
import { State } from '../domain';
import { PopupUploadEventEmitter } from '../../components/popup';
export default function (eventEmitter: PopupUploadEventEmitter): (store: MiddlewareAPI<State>) => (next: Dispatch<State>) => (action: Action) => Action;
