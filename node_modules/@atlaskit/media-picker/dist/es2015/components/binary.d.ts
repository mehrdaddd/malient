import { LocalUploadComponent, LocalUploadConfig } from './localUpload';
import { MediaPickerContext } from '../domain/context';
import { Context } from '@atlaskit/media-core';
export declare type BinaryConfig = LocalUploadConfig;
export interface BinaryUploaderConstructor {
    new (analyticsContext: MediaPickerContext, context: Context, config: BinaryConfig): BinaryUploader;
}
export declare class BinaryUploader extends LocalUploadComponent {
    constructor(analyticsContext: MediaPickerContext, context: Context, config: BinaryConfig);
    upload(base64: string, name: string): void;
    private _urltoFile(dataurl, filename);
}
