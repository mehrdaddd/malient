import { LocalUploadComponent, LocalUploadConfig } from './localUpload';
import { MediaPickerContext } from '../domain/context';
import { Context } from '@atlaskit/media-core';
export interface BrowserConfig extends LocalUploadConfig {
    readonly multiple?: boolean;
    readonly fileExtensions?: Array<string>;
}
export interface BrowserConstructor {
    new (analyticsContext: MediaPickerContext, context: Context, browserConfig: BrowserConfig): Browser;
}
export declare class Browser extends LocalUploadComponent {
    private readonly browseElement;
    constructor(analyticsContext: MediaPickerContext, context: Context, browserConfig?: BrowserConfig);
    browse(): void;
    teardown(): void;
}
