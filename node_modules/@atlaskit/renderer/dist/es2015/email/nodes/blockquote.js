import { akColorN300, akColorN40, akGridSizeUnitless, } from '@atlaskit/util-shared-styles';
import { createTag, serializeStyle } from '../util';
var css = serializeStyle({
    'border-left': "2px solid " + akColorN40,
    color: akColorN300,
    margin: akGridSizeUnitless * 1.5 + "px 0 0 0",
    'padding-left': akGridSizeUnitless * 2 + "px",
});
export default function blockquote(_a) {
    var text = _a.text;
    return createTag('blockquote', { style: css }, text);
}
//# sourceMappingURL=blockquote.js.map