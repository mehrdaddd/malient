import { reduceTree } from '../utils';
export default function heading(node, schema) {
    if (node.childCount) {
        return {
            content: reduceTree(node.content, schema),
        };
    }
    return {};
}
//# sourceMappingURL=heading.js.map