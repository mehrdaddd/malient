import { reduceTree } from '../utils';
export default function paragraph(node, schema) {
    if (node.childCount) {
        return {
            content: reduceTree(node.content, schema),
        };
    }
    return {};
}
//# sourceMappingURL=paragraph.js.map