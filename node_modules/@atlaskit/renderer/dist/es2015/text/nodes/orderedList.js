import { reduceTree } from '../utils';
export default function orderedList(node, schema) {
    if (node.childCount) {
        return {
            content: reduceTree(node.content, schema).map(function (n, i) {
                n.text = i + 1 + ". ";
                return n;
            }),
        };
    }
    return {};
}
//# sourceMappingURL=orderedList.js.map