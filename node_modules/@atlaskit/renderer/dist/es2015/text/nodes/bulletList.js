import { reduceTree } from '../utils';
export default function bulletList(node, schema) {
    if (node.childCount) {
        return {
            content: reduceTree(node.content, schema).map(function (n) {
                n.text = '* ';
                return n;
            }),
        };
    }
    return {};
}
//# sourceMappingURL=bulletList.js.map