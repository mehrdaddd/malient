/// <reference types="react" />
import { PureComponent } from 'react';
import { Schema } from 'prosemirror-model';
import { ADFStage, ProviderFactory, EventHandlers, ExtensionHandlers } from '@atlaskit/editor-common';
import { RendererContext } from '../../';
import { RenderOutputStat } from '../../';
export declare type RendererAppearance = 'message' | 'inline-comment' | 'comment' | 'full-page' | 'mobile' | undefined;
export interface Extension<T> {
    extensionKey: string;
    parameters?: T;
    content?: any;
}
export interface Props {
    document: any;
    dataProviders?: ProviderFactory;
    eventHandlers?: EventHandlers;
    extensionHandlers?: ExtensionHandlers;
    onComplete?: (stat: RenderOutputStat) => void;
    portal?: HTMLElement;
    rendererContext?: RendererContext;
    schema?: Schema;
    useNewApplicationCard?: boolean;
    appearance?: RendererAppearance;
    adfStage?: ADFStage;
}
export default class Renderer extends PureComponent<Props, {}> {
    private providerFactory;
    private serializer;
    constructor(props: Props);
    componentWillReceiveProps(nextProps: Props): void;
    private updateSerializer(props);
    render(): JSX.Element;
    componentWillUnmount(): void;
}
