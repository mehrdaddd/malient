"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var serializers_1 = require("./serializers");
exports.createTag = function (tagName, attrs, content) {
    var attrsList = [];
    Object.keys(attrs || {}).forEach(function (key) {
        var value = attrs[key];
        if (value === undefined) {
            return;
        }
        attrsList.push(key + "=\"" + String(value).replace(/"/g, '"') + "\"");
    });
    var attrsSerialized = attrsList.length ? " " + attrsList.join(' ') : '';
    return content
        ? "<" + tagName + attrsSerialized + ">" + content + "</" + tagName + ">"
        : "<" + tagName + attrsSerialized + "/>";
};
exports.serializeStyle = function (style) {
    return Object.keys(style).reduce(function (memo, key) {
        if (style[key] === undefined) {
            return memo;
        }
        var value = String(style[key]).replace(/"/g, '"');
        return (memo += key + ": " + value + ";");
    }, '');
};
exports.applyMarks = function (marks, text) {
    var output = text;
    for (var _i = 0, marks_1 = marks; _i < marks_1.length; _i++) {
        var mark = marks_1[_i];
        // ignore marks with unknown type
        if (serializers_1.markSerializers[mark.type.name]) {
            output = serializers_1.markSerializers[mark.type.name]({ mark: mark, text: output });
        }
    }
    return output;
};
//# sourceMappingURL=util.js.map