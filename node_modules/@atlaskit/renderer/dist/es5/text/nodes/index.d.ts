import { Node as PMNode, Schema } from 'prosemirror-model';
export interface ReducedNode {
    content?: ReducedNode[];
    text?: string;
}
export declare type NodeReducer = (node: PMNode, schema?: Schema) => ReducedNode;
export declare const nodeToReducerMapping: {
    [key: string]: NodeReducer;
};
