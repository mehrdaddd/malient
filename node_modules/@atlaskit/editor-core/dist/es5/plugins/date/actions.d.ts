import { EditorState, Transaction } from 'prosemirror-state';
import { DateType } from './index';
export declare const insertDate: (date?: DateType | undefined) => (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
export declare const selectElement: (element: HTMLElement | null) => (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
export declare const openDatePicker: (domAtPos: (pos: number) => any) => (state: EditorState, dispatch: (tr: Transaction) => void) => boolean;
