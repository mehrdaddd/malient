import { EditorState } from 'prosemirror-state';
import { Node as PmNode } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
import { MacroProvider, MacroAttributes } from './types';
export declare const insertMacroFromMacroBrowser: (macroProvider: MacroProvider, macroNode?: PmNode | undefined) => (view: EditorView) => Promise<boolean>;
export declare const resolveMacro: (macro?: MacroAttributes | undefined, state?: EditorState | undefined) => PmNode | null;
export declare const runMacroAutoConvert: (state: EditorState, text: String) => PmNode | null;
export declare const setMacroProvider: (provider: Promise<MacroProvider>) => (view: EditorView) => Promise<boolean>;
