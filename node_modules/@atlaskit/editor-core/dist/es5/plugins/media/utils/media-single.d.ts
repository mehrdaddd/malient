import { Node as PMNode, Schema } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
import { ImagePreview } from '@atlaskit/media-picker';
import { MediaState } from '../types';
export interface MediaSingleState extends MediaState {
    thumbnail: ImagePreview;
}
export declare const insertMediaAsMediaSingle: (view: EditorView, node: PMNode) => boolean;
export declare const insertMediaSingleNode: (view: EditorView, mediaState: MediaState, collection?: string | undefined) => boolean;
export declare const createMediaSingleNode: (schema: Schema, collection: string) => (mediaState: MediaSingleState) => PMNode;
