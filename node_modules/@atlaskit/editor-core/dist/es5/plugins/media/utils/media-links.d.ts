import { MarkType } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
import { Transaction } from 'prosemirror-state';
import { Context } from '@atlaskit/media-core';
import { MediaStateManager, MediaState } from '../types';
export interface URLInfo {
    href: string;
    pos: number;
}
export declare const insertLinks: (view: EditorView, stateManager: MediaStateManager, handleMediaState: (state: MediaState) => void, linkRanges: URLInfo[], linkCreateContext: Context, collection?: string | undefined) => Promise<(string | undefined)[] | undefined>;
export declare const detectLinkRangesInSteps: (tr: Transaction, link: MarkType, offset: number) => URLInfo[];
