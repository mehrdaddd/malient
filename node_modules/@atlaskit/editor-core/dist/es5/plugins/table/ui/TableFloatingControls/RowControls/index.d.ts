/// <reference types="react" />
import { Component } from 'react';
import { EditorView } from 'prosemirror-view';
import { Command } from '../../../../../types';
import { TableSelection } from '../utils';
export interface Props {
    editorView: EditorView;
    tableElement: HTMLElement;
    isTableHovered: boolean;
    insertRow: (row: number) => Command;
    remove: () => void;
    hoverRows: (rows: number[], danger?: boolean) => Command;
    resetHoverSelection: Command;
    scroll: number;
    updateScroll: () => void;
    isTableInDanger?: boolean;
}
export default class RowControls extends Component<Props, any> {
    state: {
        dangerRows: number[];
    };
    createDeleteRowButton(selection: any, offsetHeight: any, selectionHeight: any): JSX.Element;
    createDeleteRowButtonForSelection(selection: TableSelection, rows: any): JSX.Element;
    render(): JSX.Element | null;
    private selectRow;
    private hoverRows;
    private resetHoverSelection;
    private insertRow;
}
