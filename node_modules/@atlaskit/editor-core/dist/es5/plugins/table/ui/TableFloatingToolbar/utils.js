"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var editor_common_1 = require("@atlaskit/editor-common");
function getShortcut(keymap) {
    if (keymap) {
        var shortcutMap = { Cmd: '⌘', Shift: '⇧', Ctrl: '^', Alt: '⌥' };
        var shortcut = editor_common_1.browser.mac ? keymap.mac : keymap.windows;
        var arr = shortcut.split('-');
        return shortcutMap[arr[0]] + " + " + arr[1].toUpperCase();
    }
}
exports.getShortcut = getShortcut;
//# sourceMappingURL=utils.js.map