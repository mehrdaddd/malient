/// <reference types="react" />
import * as React from 'react';
import { EditorView } from 'prosemirror-view';
import { EventDispatcher } from '../event-dispatcher';
import { EditorState } from 'prosemirror-state';
import { EditorProps, EditorConfig, EditorPlugin } from '../types';
import { ProviderFactory, Transformer } from '@atlaskit/editor-common';
export interface EditorViewProps {
    editorProps: EditorProps;
    providerFactory: ProviderFactory;
    render?: (props: {
        editor: JSX.Element;
        view?: EditorView;
        config: EditorConfig;
        eventDispatcher: EventDispatcher;
        transformer?: Transformer<string>;
    }) => JSX.Element;
    onEditorCreated: (instance: {
        view: EditorView;
        config: EditorConfig;
        eventDispatcher: EventDispatcher;
        transformer?: Transformer<string>;
    }) => void;
    onEditorDestroyed: (instance: {
        view: EditorView;
        config: EditorConfig;
        eventDispatcher: EventDispatcher;
        transformer?: Transformer<string>;
    }) => void;
}
export default class ReactEditorView<T = {}> extends React.PureComponent<EditorViewProps & T> {
    view?: EditorView;
    eventDispatcher: EventDispatcher;
    contentTransformer?: Transformer<string>;
    config: EditorConfig;
    editorState: EditorState;
    constructor(props: EditorViewProps & T);
    componentWillReceiveProps(nextProps: EditorViewProps): void;
    /**
     * Clean up any non-PM resources when the editor is unmounted
     */
    componentWillUnmount(): void;
    getPlugins(editorProps: EditorProps): EditorPlugin[];
    createEditorState: (options: {
        props: EditorViewProps;
        replaceDoc?: boolean | undefined;
    }) => EditorState;
    createEditorView: (node: any) => void;
    handleEditorViewRef: (node: any) => void;
    render(): JSX.Element;
}
