import { Schema, MarkSpec } from 'prosemirror-model';
import { Plugin } from 'prosemirror-state';
import { ProviderFactory } from '@atlaskit/editor-common';
import { AnalyticsHandler } from '../analytics';
import { EditorPlugin, EditorProps, EditorConfig } from '../types';
import ErrorReporter from '../utils/error-reporter';
import { Dispatch, EventDispatcher } from '../event-dispatcher';
export declare function sortByRank(a: {
    rank: number;
}, b: {
    rank: number;
}): number;
export declare function fixExcludes(marks: {
    [key: string]: MarkSpec;
}): {
    [key: string]: MarkSpec;
};
export declare function processPluginsList(plugins: EditorPlugin[], editorProps: EditorProps): EditorConfig;
export declare function createSchema(editorConfig: EditorConfig): Schema;
export declare function createPMPlugins(editorConfig: EditorConfig, schema: Schema, props: EditorProps, dispatch: Dispatch, eventDispatcher: EventDispatcher, providerFactory: ProviderFactory, errorReporter: ErrorReporter): Plugin[];
export declare function createErrorReporter(errorReporterHandler: any): ErrorReporter;
export declare function initAnalytics(analyticsHandler?: AnalyticsHandler): void;
