import { EditorState } from 'prosemirror-state';
import { Side } from './selection';
import { Command } from '../../types';
export declare const enum Direction {
    BACKWARD = 0,
    FORWARD = 1,
}
export declare const arrow: (direction: Direction, endOfTextblock: (dir: string, state?: EditorState | undefined) => boolean) => Command;
export declare const deleteNode: (direction: Direction) => Command;
export declare const setGapCursorAtPos: (position: number, side?: Side) => Command;
export declare const setGapCursorForTopLevelBlocks: (event: MouseEvent, editorRef: HTMLElement, posAtCoords: (coords: {
    left: number;
    top: number;
}) => void | {
    pos: number;
    inside: number;
} | null) => Command;
