/// <reference types="react" />
import { Component } from 'react';
import { EditorView } from 'prosemirror-view';
import { Command } from '../../../../../types';
import { TableSelection } from '../utils';
export interface Props {
    editorView: EditorView;
    tableElement?: HTMLElement;
    isTableHovered: boolean;
    insertColumn: (column: number) => Command;
    hoverColumns: (columns: number[], danger?: boolean) => Command;
    resetHoverSelection: Command;
    remove: () => void;
    isTableInDanger?: boolean;
}
export default class ColumnControls extends Component<Props, any> {
    state: {
        dangerColumns: number[];
    };
    createDeleteColumnButton(selection: TableSelection, offsetWidth: any, selectionWidth: any): JSX.Element;
    createDeleteColumnButtonForSelection(selection: TableSelection, cols: any): JSX.Element;
    render(): JSX.Element | null;
    private handleMouseDown;
    private selectColumns;
    private deleteColumns(cols);
    private selectColumn;
    private hoverColumns;
    private resetHoverSelection;
    private insertColumn;
}
