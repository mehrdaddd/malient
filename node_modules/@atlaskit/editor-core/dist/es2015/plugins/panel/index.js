import * as React from 'react';
import { panel } from '@atlaskit/editor-common';
import { createPlugin, stateKey } from './pm-plugins/main';
import PanelEdit from './ui/PanelEdit';
var panelPlugin = {
    nodes: function () {
        return [{ rank: 1100, name: 'panel', node: panel }];
    },
    pmPlugins: function () {
        return [{ rank: 1110, plugin: createPlugin }];
    },
    contentComponent: function (_a) {
        var editorView = _a.editorView;
        var pluginState = stateKey.getState(editorView.state);
        return React.createElement(PanelEdit, { editorView: editorView, pluginState: pluginState });
    },
};
export default panelPlugin;
//# sourceMappingURL=index.js.map