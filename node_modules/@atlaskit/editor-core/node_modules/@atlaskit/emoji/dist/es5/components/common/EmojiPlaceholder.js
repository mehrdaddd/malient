"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var React = require("react");
var classNames = require("classnames");
var tooltip_1 = require("@atlaskit/tooltip");
var styles_1 = require("./styles");
var constants_1 = require("../../constants");
var type_helpers_1 = require("../../type-helpers");
// tslint:disable-next-line:variable-name
var EmojiPlaceholder = function (props) {
    var shortName = props.shortName, _a = props.size, size = _a === void 0 ? constants_1.defaultEmojiHeight : _a, showTooltip = props.showTooltip, representation = props.representation;
    var scaledWidth;
    var scaledHeight;
    if (representation &&
        size &&
        (type_helpers_1.isImageRepresentation(representation) ||
            type_helpers_1.isMediaRepresentation(representation))) {
        var width_1 = representation.width;
        var height_1 = representation.height;
        if (width_1 && height_1) {
            scaledWidth = size / height_1 * width_1;
            scaledHeight = size;
        }
    }
    var width = scaledWidth || size;
    var height = scaledHeight || size;
    var style = {
        fill: 'f7f7f7',
        width: width + "px",
        height: height + "px",
    };
    var classes = (_b = {},
        _b[styles_1.placeholder] = true,
        _b[styles_1.placeholderContainer] = true,
        _b);
    var placeholderNode = (React.createElement("span", { "aria-label": shortName, className: classNames(classes), style: style }));
    return showTooltip ? (React.createElement(tooltip_1.default, { tag: "span", content: shortName }, placeholderNode)) : (placeholderNode);
    var _b;
};
exports.default = EmojiPlaceholder;
//# sourceMappingURL=EmojiPlaceholder.js.map