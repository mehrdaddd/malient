import { Hasher } from './hasher';
export declare class WorkerHasher implements Hasher {
    private workers;
    private jobs;
    constructor(numOfWorkers: number);
    hash(chunk: any): void;
    private createWorker();
    private handleWorkerMessage(event, hasherWorker);
    private calculateHashInWorker(chunk);
    private dispatch(jobId, hasherWorker, chunkBlob);
    private getWorker();
}
