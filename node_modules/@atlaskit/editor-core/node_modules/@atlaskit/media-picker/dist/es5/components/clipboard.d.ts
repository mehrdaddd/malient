import { AuthProvider, Context } from '@atlaskit/media-core';
import { LocalUploadComponent } from './localUpload';
import { MediaPickerContext } from '../domain/context';
import { UploadParams } from '..';
export interface ClipboardConfig {
    uploadParams: UploadParams;
    userAuthProvider?: AuthProvider;
}
export interface ClipboardConstructor {
    new (analyticsContext: MediaPickerContext, context: Context, clipboardConfig: ClipboardConfig): Clipboard;
}
export declare class Clipboard extends LocalUploadComponent {
    constructor(analyticsContext: MediaPickerContext, context: Context, config?: ClipboardConfig);
    activate(): Promise<void>;
    deactivate(): void;
    private pasteHandler;
}
