import { Store, Dispatch } from 'redux';
import { Fetcher } from '../tools/fetcher/fetcher';
import { EditRemoteImageAction } from '../actions/editRemoteImage';
import { State } from '../domain';
export declare const editRemoteImageMiddleware: (fetcher: Fetcher) => (store: Store<State>) => (next: Dispatch<State>) => (action: EditRemoteImageAction) => EditRemoteImageAction;
export declare function editRemoteImage(fetcher: Fetcher, store: Store<State>, action: EditRemoteImageAction): Promise<void>;
