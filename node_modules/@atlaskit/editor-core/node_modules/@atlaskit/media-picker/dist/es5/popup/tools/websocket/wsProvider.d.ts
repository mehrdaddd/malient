import { Auth } from '@atlaskit/media-core';
import { WsConnectionHolder } from './wsConnectionHolder';
export declare class WsProvider {
    private connectionHolders;
    getWsConnectionHolder(apiUrl: string, auth: Auth): WsConnectionHolder;
    private createAndRemember(apiUrl, auth, tag);
    private static mapAuthToTag(auth);
}
