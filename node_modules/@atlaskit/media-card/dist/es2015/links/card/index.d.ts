/// <reference types="react" />
import { Component } from 'react';
import { UrlPreview, ImageResizeMode } from '@atlaskit/media-core';
import { SharedCardProps, CardStatus, CardAppearance } from '../..';
export interface LinkCardProps extends SharedCardProps {
    readonly status: CardStatus;
    readonly details?: UrlPreview;
    readonly resizeMode?: ImageResizeMode;
    readonly onRetry?: () => void;
}
export declare const defaultLinkCardAppearance: CardAppearance;
export declare class LinkCard extends Component<LinkCardProps, {}> {
    render(): JSX.Element | null;
    private renderInLink(link, child);
    private isURLEmbed(embed);
    private isHTMLEmbed(embed);
    private isEmbed(embed);
    private renderURLEmbed(embed);
    private renderHTMLEmbed(embed);
    private renderEmbed(embed);
    private renderSmartCard();
    private renderGenericLink(appearance);
    private renderSmallCard();
    private readonly resources;
    private readonly urlPreview;
    private readonly thumbnailUrl;
    private readonly iconUrl;
    private readonly isLoading;
    private readonly isError;
    private readonly errorMessage;
}
export default LinkCard;
