import { Observable } from "rxjs/Observable";
import { Chunkinator, ProbedBlob, ChunkinatorFile, Options, Callbacks } from "./domain";
export declare const getObservableFromFile: (file: ChunkinatorFile, options: Options, callbacks: Callbacks) => Observable<ProbedBlob[]>;
export declare const chunkinator: Chunkinator;
export declare function toCancelablePromise<T>(observable: Observable<T>): {
    response: Promise<T>;
    cancel: () => void;
};
