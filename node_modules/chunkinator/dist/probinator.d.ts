import { Observable } from "rxjs/Observable";
import "rxjs/add/observable/from";
import { HashedBlob, ProbedBlob, ProbingFunction } from "./domain";
export interface ProbinatorOptions {
    readonly batchSize: number;
    readonly prober: ProbingFunction;
}
export declare function probinator(hashedBlob$: Observable<HashedBlob>, {batchSize, prober}: ProbinatorOptions): Observable<ProbedBlob>;
